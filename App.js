import React from 'react';
import {createAppContainer} from 'react-navigation';
import ScreenManager from './view/screenManager'

const Main = createAppContainer(ScreenManager);

export default App = () => (
      <Main/>
)


